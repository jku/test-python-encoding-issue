name: test
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    
    steps:
      - run: |
          python -c "import locale; print('platform preferred encoding is', locale.getpreferredencoding())"
          python -c "open('testfile', 'w') as f; f.write('—')"
          sha256sum testfile
